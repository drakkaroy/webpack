const slider = (() => {

    const init = (component) => {

        console.log('APP.slider'); 
    }

    const test = () => {
        console.log('this is just a test');
    }

    return {
        init,
        test
    }

})();

export { slider }